# This v1 bot is public-data + local paper ledger only.
# Do not put live Kalshi credentials in source control.

# Optional: point CLI at a different config file.
KALSHI_BTC15M_CONFIG=configs/default.toml

# Optional: override local data directory.
KALSHI_BTC15M_DATA_DIR=data

# These are placeholders for a future, explicitly approved authenticated adapter.
# The current code does NOT submit live orders.
KALSHI_API_KEY_ID=
KALSHI_PRIVATE_KEY_FILE=
