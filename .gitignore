.venv/
__pycache__/
.pytest_cache/
.ruff_cache/
*.egg-info/
*.pyc
.env
*.key
*.pem

# Active local runtime state. Keep these directories in the workspace, but never commit DBs/logs/runs.
feed/*.sqlite3
feed/*.sqlite3-*
logs/
runs/

# Historical local DBs/configs/secrets moved out of the top-level workspace.
archive/local-artifacts/

# Legacy names from the pre-feed/replay layout, in case they are recreated locally.
configs/*.local.toml
data/*.sqlite3
data/*.sqlite3-*
data-live*/
runtime/
